# Screening plane. Deliberately small and cloud-agnostic -- nothing here imports a GCP
# SDK, which is what makes `make deploy` a re-point rather than a rewrite.
rapidfuzz==3.9.7
psycopg[binary]==3.2.3
httpx==0.28.1

# Adjudication plane (Gemini) and agent framework.
# Pins verified to resolve together and against the API this code actually calls:
# genai.Client(api_key=...).models.generate_content(model, contents, config) with
# response_schema + temperature. An earlier pair (0.8.0 / 0.4.0) was never installed
# and could not resolve at all -- `pip install -r requirements.txt` failed outright,
# which would have broken the reproduce path on any clean machine.
google-genai==2.18.1
google-adk==2.7.0

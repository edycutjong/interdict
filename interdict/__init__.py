"""Interdict -- autonomous sanctions-delta re-screening and interdiction."""

# Single source of truth for the version. The release workflow rewrites this exact
# line and tags the commit v<version>; tests/test_version.py asserts it stays valid
# semver and that CHANGELOG.md carries a matching entry, so the three can never drift.
__version__ = "1.0.10"
